services:
  - type: web
    name: flask-bg-remover
    runtime: python
    buildCommand: ""
    startCommand: "gunicorn app:app"
    envVars:
      - key: WEB_CONCURRENCY
        value: "1"
    plan: free
